--[[Dev Notes:
]]--

local balloon = workspace.Balloons:GetChildren()
local round = game.ReplicatedStorage.Round.Value
round = 1


local function roundFunc(plr)
	local hum = plr.Character:WaitForChild("Humanoid")
	local humWalk = hum.WalkSpeed
	local label = plr.PlayerGui:WaitForChild("ScreenGui").startTimer
	label.Visible = true
	hum.WalkSpeed = 0
	for i = 10, 0, -1 do
		print(i)
		label.Text = i
		wait(1)
	end
	hum.WalkSpeed = humWalk
	local mirrors = workspace["Mirrored Hall"].Mirrors
	label.Text = "Go!"
	wait(3)
	label.Visible = false
	mirrors:Destroy()
end

wait(10)

for v=1, #balloon do
	local random = balloon[math.random(1, #balloon)]
	random.Transparency = 0
	print(random.Name)
	wait(5)
	random:Destroy()
	balloon = workspace.Balloons:GetChildren()

end

local roundEvent = game.ReplicatedStorage.Room1["Round 2"]
round = 2
roundEvent:FireAllClients()

roundEvent.OnServerEvent:Connect(roundFunc)


